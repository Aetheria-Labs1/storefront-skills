interface:
  display_name: "Create Visual Page"
  short_description: "Create a responsive mockup with live island previews."
  default_prompt: "Use $visual-page to turn my approved plan into a visual mockup."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
