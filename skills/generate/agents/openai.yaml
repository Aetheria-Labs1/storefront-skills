interface:
  display_name: "Generate Storefront Page"
  short_description: "Create an early draft, then QA it when needed."
  default_prompt: "Use $generate to infer whether I want a fast draft or production-ready QA, then create the unpublished Lexsis draft."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
