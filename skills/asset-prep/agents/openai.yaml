interface:
  display_name: "Prepare Page Assets"
  short_description: "Search, generate, import, or replace storefront media."
  default_prompt: "Use $asset-prep to prepare storefront assets from my brief or page workspace."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
