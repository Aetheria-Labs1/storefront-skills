interface:
  display_name: "Build Storefront Draft"
  short_description: "Create the fastest useful unpublished storefront draft."
  default_prompt: "Use $build to create a fast unpublished Lexsis draft from this prompt and optional template URL."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"

