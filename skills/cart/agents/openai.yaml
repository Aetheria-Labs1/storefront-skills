interface:
  display_name: "Configure Cart Profiles"
  short_description: "Configure cart profiles, rules, and upsells."
  default_prompt: "Configure a cart profile for my storefront."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
