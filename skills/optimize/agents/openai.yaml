interface:
  display_name: "Optimize Storefront Page"
  short_description: "Score an existing page and apply an approved optimization plan."
  default_prompt: "Use $optimize to score this page against the build rules, propose a strict optimization plan, and apply the approved changes with hosted QA."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
