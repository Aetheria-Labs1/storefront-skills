interface:
  display_name: "Plan Storefront Page"
  short_description: "Plan a storefront around shopper decisions."
  default_prompt: "Use $plan-page to create an intent-aware page plan grounded in shopper behavior and current store evidence."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
