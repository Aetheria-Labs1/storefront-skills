interface:
  display_name: "Plan Storefront Page"
  short_description: "Finalize page strategy, sections, copy, proof, and asset requirements."
  default_prompt: "Use $plan-page to create an approval-ready storefront page specification with final copy, responsive sections, proof, and production asset requirements."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
