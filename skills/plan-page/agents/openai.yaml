interface:
  display_name: "Plan Storefront Page"
  short_description: "Plan a storefront at the depth the request needs."
  default_prompt: "Use $plan-page to infer whether this needs a fast draft plan or a production-ready plan."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
