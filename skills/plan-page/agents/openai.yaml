interface:
  display_name: "Plan Storefront Page"
  short_description: "Produce the complete page specification and wait for approval."
  default_prompt: "Use $plan-page to produce the complete specification for this page: final section copy, an asset decision per section, claim gate, work queue and plan status, then wait for my approval."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
