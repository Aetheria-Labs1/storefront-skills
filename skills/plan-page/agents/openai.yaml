interface:
  display_name: "Plan Storefront Page"
  short_description: "Create an approved storefront page blueprint before generation."
  default_prompt: "Plan a high-converting storefront page for this brief."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
