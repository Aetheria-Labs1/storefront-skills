interface:
  display_name: "Set Up Storefront"
  short_description: "Build reusable brand, product, persona, rules, design, and theme context."
  default_prompt: "Use $setup to build or refresh the structured Lexsis store context I want later page skills to reuse."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
