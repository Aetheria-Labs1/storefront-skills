interface:
  display_name: "Design Page"
  short_description: "Compose the real page source and interactive preview."
  default_prompt: "Use $design-page to turn my approved plan into a responsive page design."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
