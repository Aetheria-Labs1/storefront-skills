interface:
  display_name: "Design Page"
  short_description: "Build the approved plan into one hosted draft with hosted QA."
  default_prompt: "Use $design-page to build the approved plan into canonical source and one unpublished hosted draft, run hosted QA at 390, 768 and 1280, and return DESIGN_APPROVED."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
