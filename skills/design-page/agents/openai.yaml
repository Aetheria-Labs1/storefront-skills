interface:
  display_name: "Design Page"
  short_description: "Implement the approved plan and verified assets as one hosted draft."
  default_prompt: "Use $design-page to implement my PLAN_APPROVED specification with ASSETS_READY bindings, create or update one unpublished hosted draft, run hosted QA at 390, 768 and 1280, and return DESIGN_APPROVED."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
