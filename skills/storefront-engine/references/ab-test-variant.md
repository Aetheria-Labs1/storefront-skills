# A/B test variants

Use `references/ab-testing.md` for URL-first control resolution, one-hypothesis challengers, source-first edits, and draft experiment handoffs. It owns the variant lifecycle; publication remains a separate explicit action.
