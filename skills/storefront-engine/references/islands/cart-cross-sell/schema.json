{
  "name": "CartCrossSell",
  "category": "unknown",
  "usage_hint": "Self-contained cross-sell/upsell inside the cart drawer. Reads config from $cartConfig.upsells, evaluates triggers against $cartLines, fetches recommended products, and self-hides when no triggers match. No external rules needed.",
  "when_to_use": "Inside DrawerShell to show product recommendations based on cart contents. Config comes from commerce_config.upsells \u2014 the island is fully reactive to cart state changes.",
  "when_not_to_use": "Outside cart context. For static product grids use ProductCarousel.",
  "defaultHydrate": "immediate",
  "combine_with": [
    "DrawerShell",
    "CartLines",
    "CartSummary",
    "CartCheckoutButton",
    "CartProgressBar"
  ],
  "anti_patterns": [
    "Passing product data via data-props (use commerce_config.upsells instead \u2014 the island reads from $cartConfig)",
    "Using hidden class + rules for visibility (the island self-manages show/hide)",
    "Placing outside DrawerShell (needs cart state subscription)"
  ],
  "examples": [
    {
      "label": "Default \u2014 reads upsells from store config, no props needed",
      "html": "<div data-island=\"CartCrossSell\" data-props='{}'></div>"
    },
    {
      "label": "With static override (backward compat)",
      "html": "<div data-island=\"CartCrossSell\" data-props='{\"heading\":\"Complete your routine\",\"layout\":\"horizontal\"}'></div>"
    }
  ],
  "props": {
    "heading": {
      "type": "string",
      "required": false
    },
    "max": {
      "type": "number",
      "required": false,
      "default": "3"
    },
    "layout": {
      "type": "enum",
      "required": false,
      "values": [
        "horizontal",
        "grid",
        "stack"
      ],
      "default": "horizontal"
    },
    "showQuickAdd": {
      "type": "boolean",
      "required": false,
      "default": "true"
    }
  },
  "variants": [
    "horizontal",
    "grid",
    "stack"
  ],
  "css_vars": [
    "--lx-accent-color",
    "--lx-border-color",
    "--lx-text-color"
  ],
  "parts": [
    "add-button",
    "heading",
    "item",
    "item-image",
    "item-info",
    "item-price",
    "item-title",
    "root"
  ],
  "example": "<div data-island=\"CartCrossSell\" data-props='{}'></div>"
}
