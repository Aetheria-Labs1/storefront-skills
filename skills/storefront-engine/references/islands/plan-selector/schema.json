{
  "contract_version": 2,
  "name": "PlanSelector",
  "category": "commerce",
  "capabilities": [
    {
      "name": "product:resolve"
    }
  ],
  "usage_hint": "Subscribe-and-save plan cards \u2014 large radio cards (Autoship vs One-Time) with per-card price, compare-at, per-unit cost, benefit checklist, and live re-pricing from a tier selector. The dominant DTC PDP purchase pattern.",
  "when_to_use": "On PDPs offering a subscription plan alongside one-time purchase, styled as prominent selectable cards. Emits subscription:changed so a BuyBox with listenForEvents picks up the selling plan and price with no extra wiring. Set listenForTiers:true to re-price cards live from a QuantityBreaks selector (emitEvents:true). Add data-island-container to the placement div to inject custom HTML (e.g. a QuantityBreaks island) inside the first plan card body.",
  "when_not_to_use": "For a lightweight inline toggle use SubscriptionToggle. For quantity-only discounts without a subscription option use QuantityBreaks alone. Not a cart CTA \u2014 pair with BuyBox or ProceedToCart for add-to-cart.",
  "defaultHydrate": "immediate",
  "combine_with": [
    "QuantityBreaks",
    "BuyBox",
    "Cart V2 via head.use_cart_v2",
    "PaymentOptions"
  ],
  "anti_patterns": [
    "Using together with SubscriptionToggle on the same page (both emit subscription:changed \u2014 they fight)",
    "Omitting a BuyBox/ProceedToCart CTA (PlanSelector selects, it does not add to cart)",
    "Styling selection via inline overrides (target [data-part=\"plan\"][data-selected=\"true\"] in CSS instead)",
    "Doing price math in props at runtime (pass pre-formatted strings; tiers:changed payloads carry pre-computed subscribe prices)"
  ],
  "props": {
    "plans": {
      "type": "array",
      "items": {
        "type": "object",
        "shape": {
          "id": "string",
          "title": "string",
          "price": "string",
          "compareAtPrice": "?string",
          "perUnit": "?string",
          "sellingPlanId": "?string",
          "frequency": "?string",
          "benefits": "?array<string>",
          "note": "?string",
          "badge": "?string",
          "static": "?boolean | boolean"
        }
      },
      "shape": {
        "id": "string",
        "title": "string",
        "price": "string",
        "compareAtPrice": "?string",
        "perUnit": "?string",
        "sellingPlanId": "?string",
        "frequency": "?string",
        "benefits": "?array<string>",
        "note": "?string",
        "badge": "?string",
        "static": "?boolean | boolean"
      },
      "required": true
    },
    "defaultPlanId": {
      "type": "string",
      "required": false
    },
    "variant": {
      "type": "enum",
      "values": [
        "stacked",
        "cards",
        "minimal"
      ],
      "required": false,
      "default": "cards"
    },
    "listenForTiers": {
      "type": "boolean",
      "required": false,
      "default": "false"
    },
    "emitEvents": {
      "type": "boolean",
      "required": false,
      "default": "true"
    },
    "showBenefits": {
      "type": "boolean",
      "required": false,
      "default": "true"
    },
    "__innerHTML": {
      "type": "string",
      "required": false,
      "default": ""
    }
  },
  "variants": [
    "stacked",
    "cards",
    "minimal"
  ],
  "css_vars": [
    "--lx-accent-color",
    "--lx-border-color",
    "--lx-surface",
    "--lx-text-color"
  ],
  "parts": [
    "benefit-item",
    "cadence-line",
    "plan",
    "plan-badge",
    "plan-benefits",
    "plan-compare",
    "plan-note",
    "plan-per-unit",
    "plan-price",
    "plan-radio",
    "plan-slot",
    "plan-title",
    "root"
  ],
  "authoring": {
    "format": "source-html",
    "element": "lx-island",
    "props_encoding": "script[type=application/json]",
    "allowed_attributes": [
      "class",
      "id",
      "style",
      "hydrate"
    ],
    "headless": {
      "supported": false
    }
  }
}
