{
  "contract_version": 2,
  "name": "Marquee",
  "category": "hero",
  "capabilities": [],
  "deprecated": true,
  "replacement": "CSS animation with static HTML",
  "deprecation_reason": "This component has no Shopify integration and its behavior is expressible with HTML and CSS.",
  "usage_hint": "Compatibility contract for existing pages. Do not author this retired island in new source.",
  "when_to_use": "Inspect an existing page only; use the replacement in workflows/island-selection-workflow.md for new source.",
  "when_not_to_use": "Do not use for new page authoring.",
  "defaultHydrate": "visible",
  "combine_with": [],
  "anti_patterns": [
    "Too many items with long text (visual clutter \u2014 keep items short)",
    "speed < 10 with few items (jerky/too fast \u2014 use speed 20-40 for smooth feel)",
    "Using for critical CTA content users must read (they can't pause reliably on mobile)",
    "Mixing logos and text items without consistent height (misaligned row)"
  ],
  "props": {
    "variant": {
      "type": "enum",
      "values": [
        "text",
        "logos",
        "badges",
        "mixed"
      ],
      "required": false
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "shape": {
          "html": "?string",
          "text": "?string",
          "imageSrc": "?string",
          "imageAlt": "?string"
        }
      },
      "shape": {
        "html": "?string",
        "text": "?string",
        "imageSrc": "?string",
        "imageAlt": "?string"
      },
      "required": true
    },
    "speed": {
      "type": "number",
      "required": false
    },
    "direction": {
      "type": "enum",
      "values": [
        "left",
        "right"
      ],
      "required": false
    },
    "pauseOnHover": {
      "type": "boolean",
      "required": false
    },
    "gap": {
      "type": "string",
      "required": false
    },
    "repeat": {
      "type": "number",
      "required": false
    },
    "separator": {
      "type": "string",
      "required": false
    },
    "height": {
      "type": "string",
      "required": false
    },
    "bgColor": {
      "type": "string",
      "required": false
    },
    "textColor": {
      "type": "string",
      "required": false
    },
    "fontSize": {
      "type": "string",
      "required": false
    },
    "fontWeight": {
      "type": "string",
      "required": false
    },
    "logoHeight": {
      "type": "string",
      "required": false
    },
    "grayscale": {
      "type": "boolean",
      "required": false
    },
    "opacity": {
      "type": "number",
      "required": false
    },
    "padding": {
      "type": "string",
      "required": false
    }
  },
  "variants": [
    "text",
    "logos",
    "badges",
    "mixed"
  ],
  "css_vars": [],
  "parts": [
    "root"
  ],
  "authoring": {
    "format": "source-html",
    "element": "lx-island",
    "props_encoding": "script[type=application/json]",
    "allowed_attributes": [
      "class",
      "id",
      "style",
      "hydrate"
    ],
    "headless": {
      "supported": false
    }
  }
}
