{
  "contract_version": 2,
  "name": "SubscriptionToggle",
  "category": "commerce",
  "capabilities": [
    {
      "name": "product:resolve"
    }
  ],
  "usage_hint": "Subscribe-and-save toggle that switches between one-time purchase and subscription pricing. Emits subscription:changed events to BuyBox.",
  "when_to_use": "On product pages with subscription selling plans. Auto-emits state changes so BuyBox/BuyBox or ProceedToCart islands can update checkout behavior. Shows discount, frequency options, and benefits list.",
  "when_not_to_use": "For products without subscription options. For simple quantity selectors use BuyBox quantity field. For tiered pricing use QuantityBreaks instead.",
  "defaultHydrate": "immediate",
  "combine_with": [
    "BuyBox",
    "BuyBox or ProceedToCart",
    "ProductPrice (all listen for subscription:changed events)"
  ],
  "anti_patterns": [
    "Not providing valid Shopify selling plan IDs \u2014 checkout will fail",
    "Listing benefits that don't match actual subscription terms (misleading)",
    "Using alongside QuantityBreaks (they conflict \u2014 choose one pricing model)"
  ],
  "props": {
    "oneTimePrice": {
      "type": "string",
      "required": true
    },
    "plans": {
      "type": "array",
      "items": {
        "type": "object",
        "shape": {
          "id": "string",
          "frequency": "string",
          "discount": "string",
          "price": "string"
        }
      },
      "shape": {
        "id": "string",
        "frequency": "string",
        "discount": "string",
        "price": "string"
      },
      "required": true
    },
    "benefits": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": false
    },
    "defaultMode": {
      "type": "enum",
      "values": [
        "one-time",
        "subscribe"
      ],
      "required": false,
      "default": "one-time"
    },
    "label": {
      "type": "string",
      "required": false
    }
  },
  "variants": null,
  "css_vars": [
    "--lx-accent-color",
    "--lx-text-color"
  ],
  "parts": [
    "benefit-item",
    "benefits",
    "frequency-select",
    "label",
    "option-one-time",
    "option-subscribe",
    "root"
  ],
  "authoring": {
    "format": "source-html",
    "element": "lx-island",
    "props_encoding": "script[type=application/json]",
    "allowed_attributes": [
      "class",
      "id",
      "style",
      "hydrate"
    ],
    "headless": {
      "supported": false
    }
  }
}
