{
  "contract_version": 2,
  "name": "Tabs",
  "category": "content",
  "capabilities": [],
  "deprecated": true,
  "replacement": "CSS-only tabs with radio inputs or <details>",
  "deprecation_reason": "Generic tabs have no Shopify integration and can be authored with accessible HTML and CSS.",
  "usage_hint": "Compatibility contract for existing pages. Do not author this retired island in new source.",
  "when_to_use": "Inspect an existing page only; use the replacement in workflows/island-selection-workflow.md for new source.",
  "when_not_to_use": "Do not use for new page authoring.",
  "defaultHydrate": "immediate",
  "combine_with": [],
  "anti_patterns": [],
  "props": {
    "tabs": {
      "type": "array",
      "items": {
        "type": "object",
        "shape": {
          "label": "string",
          "content": "string",
          "icon": "?string"
        }
      },
      "shape": {
        "label": "string",
        "content": "string",
        "icon": "?string"
      },
      "required": true
    },
    "defaultTab": {
      "type": "number",
      "required": false,
      "default": "0"
    },
    "style": {
      "type": "enum",
      "values": [
        "pills",
        "underline",
        "bordered"
      ],
      "required": false,
      "default": "underline"
    }
  },
  "variants": [
    "pills",
    "underline",
    "bordered"
  ],
  "css_vars": [
    "--lx-accent-color"
  ],
  "parts": [
    "panel",
    "root",
    "tab",
    "tab-list"
  ],
  "authoring": {
    "format": "source-html",
    "element": "lx-island",
    "props_encoding": "script[type=application/json]",
    "allowed_attributes": [
      "class",
      "id",
      "style",
      "hydrate"
    ],
    "headless": {
      "supported": false
    }
  }
}
