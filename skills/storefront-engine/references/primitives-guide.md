# Source primitives

Use `references/authoring/source-authoring.md` for static HTML, `<lx-island>` with its `hydrate` attribute, fallback content, and supported headless behavior. Resolve interactive requirements through `references/workflows/island-selection-workflow.md`; native markup handles non-interactive content.
