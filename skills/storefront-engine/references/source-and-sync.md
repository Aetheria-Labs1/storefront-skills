# Source and synchronization

Use `references/source-artifact-workflow.md` for exact-input hashes and synchronization, `references/generation-protocol.md` for draft/version transitions, and `references/workflow-intent.md` for routing evidence. The existing draft is reused when its binding exists; a failed production check is reported without erasing the reversible draft.
