---
description: QA check and publish a page — validates structure, verifies islands, checks mobile rendering, then publishes live or to Shopify
allowed-tools: mcp__lexsis-ai__*
---

# /publish

QA check and publish a page — validates structure, verifies islands, checks mobile rendering, then publishes live.

## Context

- **qa-recipe**: QA checklist for pre-publish verification.

## Workflow

See `reference/publishing.md` for the full publish workflow (validate → preview → visual verify → publish live).
