interface:
  display_name: "Build with Template"
  short_description: "Turn a chosen template directly into a storefront draft."
  default_prompt: "Use $build-with-template to adapt this template URL and create an unpublished Lexsis draft."
dependencies:
  tools:
    - type: "mcp"
      value: "lexsis-ai"
      description: "Lexsis AI storefront MCP server"
      transport: "streamable_http"
      url: "https://mcp.trylexsis.com/mcp"
